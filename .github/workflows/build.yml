name: "Build Khayel APK with Cache"

on:
  push: 
    branches:
      - main
  workflow_dispatch: 

env:
  ANDROID_HOME: /home/runner/android-sdk
  GODOT_VERSION: 4.6.2

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install openjdk-17-jdk wget unzip sed -y
          pip install --upgrade gdown

      - name: Cache Android SDK
        uses: actions/cache@v4
        with:
          path: ${{ env.ANDROID_HOME }}
          key: ${{ runner.os }}-android-sdk-v1

      - name: Cache Godot & Templates
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/share/godot/export_templates
            ./godot
          key: ${{ runner.os }}-godot-${{ env.GODOT_VERSION }}-v1

      - name: Setup Android SDK
        run: |
          if [ ! -d "$ANDROID_HOME/cmdline-tools" ]; then
            mkdir -p $ANDROID_HOME/cmdline-tools
            cd $ANDROID_HOME/cmdline-tools
            wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
            unzip -q commandlinetools-linux-11076708_latest.zip
            mv cmdline-tools latest
            yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME "platform-tools" "build-tools;34.0.0" "platforms;android-34"
          fi

      - name: Download Project from Google Drive
        run: |
          FILE_ID="1WqPJVISx4dlJtZHERoZQ1m_J1MyXbR7x"
          gdown "https://drive.google.com/uc?id=${FILE_ID}" -O khayel.zip
          unzip -qo khayel.zip -d .

      - name: Setup Godot Engine
        run: |
          if [ ! -f "./godot" ]; then
            wget -q https://github.com/godotengine/godot-builds/releases/download/4.6.2-stable/Godot_v4.6.2-stable_linux.x86_64.zip
            unzip -qo Godot_v4.6.2-stable_linux.x86_64.zip
            mv Godot_v4.6.2-stable_linux.x86_64 godot
            chmod +x godot
          fi

      - name: Setup Export Templates
        run: |
          TEMPLATE_PATH="$HOME/.local/share/godot/export_templates/4.6.2.stable"
          if [ ! -d "$TEMPLATE_PATH" ]; then
            mkdir -p "$TEMPLATE_PATH"
            wget -q https://github.com/godotengine/godot-builds/releases/download/4.6.2-stable/Godot_v4.6.2-stable_export_templates.tpz
            unzip -qo Godot_v4.6.2-stable_export_templates.tpz
            mv templates/* "$TEMPLATE_PATH/"
          fi

      - name: Auto-Patch Project Files
        run: |
          PROJECT_ROOT="khayel"
          PRESET_FILE="$PROJECT_ROOT/export_presets.cfg"
          
          # تأكيد موقع الـ JKS ونسخه (موجود في الجذر حسب الصورة)
          KEY_SOURCE=$(find . -maxdepth 1 -name "lamoushi.keystore.jks")
          cp "$KEY_SOURCE" "$PROJECT_ROOT/lamoushi.keystore.jks"
          ABS_KEY=$(realpath "$PROJECT_ROOT/lamoushi.keystore.jks")

          echo "🛠️ تنظيف وتجهيز الملفات..."
          # حذف ملفات الـ UID والـ Import القديمة لضمان توافق البيئة
          find "$PROJECT_ROOT" -name "*.uid" -delete
          find "$PROJECT_ROOT" -name "*.import" -delete

          # ترقيع ملف الإعدادات بشكل عدواني لضمان ملء كل الفراغات
          sed -i 's/gradle_build\/min_sdk=""/gradle_build\/min_sdk="24"/g' "$PRESET_FILE"
          sed -i 's/gradle_build\/target_sdk=""/gradle_build\/target_sdk="34"/g' "$PRESET_FILE"
          
          # حقن التوقيع
          sed -i '/keystore\/debug/d' "$PRESET_FILE"
          sed -i '/keystore\/release/d' "$PRESET_FILE"
          sed -i "/package\/signed=true/a keystore/debug=\"$ABS_KEY\"\nkeystore/debug_user=\"lamoushi_key\"\nkeystore/debug_password=\"lamoushi2026\"\nkeystore/release=\"$ABS_KEY\"\nkeystore/release_user=\"lamoushi_key\"\nkeystore/release_password=\"lamoushi2026\"" "$PRESET_FILE"
          
          # ضمان تفعيل المعماريات
          sed -i 's/architectures\/arm64-v8a=false/architectures\/arm64-v8a=true/g' "$PRESET_FILE"

          # تحديث ملف الـ .gdextension
          cat <<EOF > "$PROJECT_ROOT/addons/gdffmpeg/gdffmpeg.gdextension"
          [configuration]
          entry_symbol = "gdffmpeg_init"
          compatibility_minimum = "4.1"
          [libraries]
          android.release.arm64 = "res://addons/gdffmpeg/bin/libgdffmpeg.android.arm64.so"
          android.debug.arm64   = "res://addons/gdffmpeg/bin/libgdffmpeg.android.arm64.so"
          EOF

 
      # خطوة إضافية لضمان قبول الاتفاقيات (Licenses)
      - name: Accept Android Licenses
        run: yes | ${{ env.ANDROID_HOME }}/cmdline-tools/latest/bin/sdkmanager --licenses



      - name: Final Build APK
        run: |
          mkdir -p build/android
          # حذف الكاش القديم
          rm -rf khayel/.godot
          
          echo "🔄 الخطوة 1: استيراد الموارد (Import)..."
          ./godot --headless --path khayel --editor --quit --verbose || true
          
          # انتظار 5 ثوانٍ لضمان استقرار ملفات النظام
          sleep 5 
          
          echo "🚀 الخطوة 2: التصدير (Export)..."
          # استخدام --export-debug مع التأكد من اسم الـ Preset "Android"
          ./godot --headless --path khayel --export-debug "Android" build/android/Khayel_Final.apk --verbose


      - name: Create Godot Editor Settings
        run: |
          mkdir -p ~/.config/godot
          echo '[gd_resource type="EditorSettings" format=3]
          [resource]
          export/android/sdk_path = "${{ env.ANDROID_HOME }}"
          export/android/android_sdk_path = "${{ env.ANDROID_HOME }}"
          export/android/jdk_path = "/usr/lib/jvm/java-17-openjdk-amd64"' > ~/.config/godot/editor_settings-4.6.tres

      - name: Final Build APK
        run: |
          mkdir -p build/android
          # مسح الكاش تماماً لإجبار المحرك على إعادة استيراد كل شيء (بدون ملفات .import القديمة)
          rm -rf khayel/.godot
          
          echo "🔄 إعادة بناء قاعدة بيانات المشروع..."
          ./godot --headless --path khayel --editor --quit --verbose || true
          
          echo "🚀 التصدير النهائي للأندرويد..."
          ./godot --headless --path khayel --export-debug "Android" build/android/Khayel_Final.apk --verbose



      - name: Create Godot Editor Settings
        run: |
          mkdir -p ~/.config/godot
          echo '[gd_resource type="EditorSettings" format=3]
          [resource]
          export/android/sdk_path = "${{ env.ANDROID_HOME }}"
          export/android/android_sdk_path = "${{ env.ANDROID_HOME }}"
          export/android/jdk_path = "/usr/lib/jvm/java-17-openjdk-amd64"' > ~/.config/godot/editor_settings-4.6.tres

      - name: Final Build APK
        run: |
          mkdir -p build/android
          # تنظيف الكاش لإجبار المحرك على قراءة الترقيعات الجديدة
          rm -rf khayel/.godot
          
          echo "🔄 جاري استيراد الموارد وتحديث المشروع..."
          ./godot --headless --path khayel --editor --quit --verbose || true
          
          echo "🚀 بدء عملية التصدير النهائية..."
          ./godot --headless --path khayel --export-debug "Android" build/android/Khayel_Final.apk --verbose

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Khayel-APK-Package
          path: build/android/Khayel_Final.apk
